query CartInput {
  cart {
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
  }
  discount {
    metafield(namespace: "$app", key: "function-configuration") {
      value
    }
  }
}
